# 移除 RetroGame ONNX 动作分类链

## 当前状态

| 项 | 结论 |
| --- | --- |
| 目标 | 移除托管 ONNX Runtime 与网球动作分类功能，同时保留 `detect_pose`、YOLO 与 RKNN 姿态检测能力。 |
| 状态 | ✅ 完成（用户明确不执行保留姿态检测链的运行验证） |
| 已确认运行链 | `InferenceEngineHandler → TennisInferenceEngine → ONNXModelLoader → Microsoft.ML.OnnxRuntime → pose_classifier.onnx`。 |
| 明确保留 | `Assets/Plugins/x64/Detect/`、`Assets/Plugins/Android/Detect/`、`Assets/StreamingAssets/model/*.rknn.enc` 及其 GameCore/Pose 数据输入链。 |
| 用户改动边界 | 当前工作树已有广泛的 Unity 导入/业务改动和 UnityMCP 删除暂存；本任务只触碰 ONNX 分类链所属路径。 |

## Task Contract

| 目标结果 | Invariants | 允许范围 | 验收 | 回退 |
| --- | --- | --- | --- |
| 项目不再编译、打包或加载托管 ONNX 动作分类。 | 保留姿态数据接收、骨架渲染、`detect_pose`/YOLO/RKNN 原生检测和无关用户改动。 | 删除分类链代码、模型、托管/平台 ONNX Runtime、构建处理器及已确认的场景组件；修改直接 consumer 以解除编译依赖。 | 语义/GUID 残留扫描、Unity 编译；用户明确不执行姿态检测冒烟验证。 | 所有删除由 Git 恢复；若姿态检测回归，恢复本任务范围，不恢复无关改动。 |

## 执行与验证

| 阶段 | 工作项 | 状态 | 证据/下一步 |
| --- | --- | --- | --- |
| 事实与边界 | 区分动作分类与原生姿态检测 | ✅ 完成 | 分类链与 `detect_pose → onnxruntime.dll → yolo8s-pose.onnx` 已分离。 |
| 独立复核 | 确认代码、资源和场景删除面 | ✅ 完成 | 降级复核确认分类链、两个 Scene 组件与需保留的 native detection 边界。 |
| 实施 | 删除分类链并解除业务 consumer | ✅ 完成 | 已移除托管 Runtime、分类模型、分类代码、Editor 构建工具及直接 consumer；保留 `x64/Detect`、Android `Detect` 和 RKNN 模型。 |
| 定向验证 | 扫描残留与 Unity 编译 | ✅ 完成 | Unity Editor 已移除 `MyDown` 和 `pose` 的 `PoseAPIManager` 缺失分类组件，以及 `pose` 的 `InferenceResultDisplay`；场景/实现残留扫描为零，随后 `Tundra build success (0.81 seconds)` 并完成 Domain Reload。 |
| 回归 | 验证保留的姿态检测 | ⏭️ 按用户要求跳过 | 用户明确无需执行 `detect_pose`/YOLO/RKNN 输入与骨架渲染的运行验证；native detection 文件仍保留。 |
