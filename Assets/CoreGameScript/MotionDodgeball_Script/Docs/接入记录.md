# MotionDodgeball 接入记录

- 游戏标识：MotionDodgeball
- 主场景：MotionDodgeball_Main
- 代码目录：`Assets/CoreGameScript/MotionDodgeball_Script`
- 独立程序集：`AssemblyMotionDodgeball`；按篮球、网球、足球、保龄球的项目模块方式维护。
- AssemblyHotList 条目：不复用 `AssemblyUpdateScriptsHOT`；若后续要通过 HybridCLR 下发此模块，须在发布配置中新增 `AssemblyMotionDodgeball` 并完成目标设备读回。
- AssetBundle 分组：MotionDodgeball_Main（待 Unity Editor 中设置并回读确认）
- 入口菜单/加载方式：`GameResManager.LoadScene(GameResManager.SceneID.MotionDodgeball_Main)`；大厅菜单序列化配置待补充。
- PoseAPI 需求：当前迁入版本使用键盘输入作 Editor 回归；体感 Pose 输入适配和目标设备验证待完成。
- 公共资源依赖：无；场景使用内置 2D/URP 组件。
- 首次发布版本与资源版本：待填写。
- 负责人：待填写。

## 本次迁移边界

- 使用独立 `AssemblyMotionDodgeball.asmdef`，使玩法代码不再混入通用 HOT 模块。
- 源项目的 `Editor/SkillLabSceneBuilder.cs` 不迁入；场景和 Prefab 由源项目已保存的资产复制而来。
- 迁入资源保持游戏私有目录，不写入 `StreamingAssets`。
