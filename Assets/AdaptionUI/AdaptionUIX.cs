using System.Collections;
using System.Collections.Generic;
using System.Text.RegularExpressions;
using UnityEngine;

public class AdaptionUIX : MonoBehaviour
{
    public bool isLeft = false;

    private void Start()
    {
        //Transform objs = this.transform;
        //float X = 960 - Mathf.Abs(objs.localPosition.x);
        //float Widthnew = (float)Screen.height * (1900f/1200f);
 
        //if (isLeft)
        //{

        //    objs.localPosition = new Vector3(-(Widthnew / 2) + X-((Screen.width- Widthnew)/2), objs.localPosition.y, objs.localPosition.z);
        //}
        //else
        //{

        //    objs.localPosition = new Vector3((Widthnew / 2) - X + ((Screen.width - Widthnew) / 2), objs.localPosition.y, objs.localPosition.z);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
